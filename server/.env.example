# Server configuration
SERVER_PORT=3000

# Path to the data directory containing school/student JSON files
DATA_DIR=./data

# Optional: restrict CORS to specific origin (leave empty to allow all)
# CORS_ORIGIN=http://localhost:8080
